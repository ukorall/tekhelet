# בורר קשירת תכלת - מסמך עיצוב (v0.1)

מסמך זה מתעד את התשתית שנבנתה עבור אפליקציית אנדרואיד שעוזרת לבחור שיטת קשירה
בתכלית, אחרי שכבר הוחלט לקשור תכלת. הוא מבוסס על שתי השיחות שצורפו (עם רועי
ועם יהונתן), שבהן אוריאל ליווה חבר בתהליך בחירה באמצעות שאלון ערכים ודיון.
המטרה: להפוך תהליך ידני-אישי הזה לכלי חוזר, תוך שמירה על אותה שיטת חשיבה.

בהמשך מתועד גם קוד שלד עובד (Kotlin + Jetpack Compose) שממש את העיצוב הזה
בפועל, ראו "מצב הקוד" בתחתית המסמך. חלק מהתוכן (בעיקר סעיף 2 וסעיף 6) מבוסס
גם על מסמך "הוראות שימוש לתיקיית קורס תכלת" (docx) שאוריאל שלח - מדריך
לתיקיית המקורות שלו, שכולל גם ניתוח ממשי לשלוש השאלות (האם/כמה/איך).

## 1. עקרון מנחה

מהשיחות עולה תהליך דו-שלבי:
1. **שאלון ערכים** (1-10) שממפה מה חשוב למשתמש - זה מצמצם מהר מאוד מתוך כל
   השיטות האפשריות לכמה מועמדות סבירות.
2. **בירור נקודתי** כשנשארות כמה מועמדות קרובות - כולל שאלות "מכוונות" נוספות
   (זיקה חסידית, כמה כריכות), והכרעה חזותית סופית ("מה הכי יפה בעיניך?").

האפליקציה משחזרת את שני השלבים: שאלון ראשי שתמיד נשאל, ושאלות משנה מותנות
שמופיעות רק כשבאמת נדרשות (לא מציפים את המשתמש בשאלות שלא רלוונטיות אליו).

חשוב: זה כלי עזר לבחירה מתוך אפשרויות לגיטימיות, לא פסק הלכה. הדבר היחיד שאמור
להיות "קשיח" בפועל הוא קשר עליון ותחילת/סיום בלבן (כפי שצוין בשיחה) - שיקול
זה מופיע כהערה במסך התוצאות ולא כמנוע החלטה.

## 2. חמשת אזורי האפליקציה (Topics)

האפליקציה בנויה סביב חמישה אזורים, **בסדר הלוגי של התהליך**. הם ממומשים
כ-enum בודד `Topic` (`model/Topic.kt`):

1. **האם?** - להטיל תכלת בכלל, כולל שאלת זיהוי החילזון (ארגמון מול דיונון
   ראדזין) ושאלת הצבע. עדיין אין כאן שאלון פעיל, אבל יש תוכן אמיתי.
2. **כמה?** - כמה מתוך שמונת החוטים יהיו תכלת. גם כאן עדיין תוכן ולא שאלון.
3. **איך?** - לפי איזו שיטה לקשור. זה החלק הממומש במלואו.
4. **איך בפועל** - מה לקנות ואיך לקשור צעד אחר צעד, כולל מחשבון אורך חוט.
5. **עיון נוסף והרחבות** - סוגיות שכדאי לפתוח, שאלות נפוצות, וחומר לעיון.
   זה גם המקום של מצב הלימוד.

**המסע** (`model/Journey.kt`) עובר בין חמשת האזורים לפי הסדר, ואפשר לעצור
בכל שלב - המצב נשמר ב-`SessionStore` וממשיכים ממנו בפעם הבאה.

לאזורים שעדיין אין בהם שאלון פעיל יש `notes` - לא "בקרוב" ריק, אלא מה שהיה
נאמר בשיחה על הנושא. מוצג ב-`TopicNotesScreen`.

## 3. השאלון - חידוד והרחבה

### שאלות הבסיס (מהשיחות, מנוסחות מחדש)

| # | שאלה | מדוע |
|---|------|------|
| 1 | עד כמה חשוב לך שהקשירה תהיה זולה? | עלות חוט/עבודה |
| 2 | עד כמה חשוב לך שהקשירה תהיה יפה? | "זה אלי ואנוהו" |
| 3 | עד כמה חשוב לך ביסוס בראשונים? | סמכות הלכתית קלאסית |
| 4 | עד כמה חשוב לך ביסוס מקורות כללי (כולל גאונים/אחרונים/מחקר)? | סמכות רחבה יותר מראשונים בלבד |
| 5 | עד כמה חשוב לך שהשיטה תהיה נפוצה ולא מיוחדת מדי? | קונצנזוס/נוחות חברתית |
| 6 | עד כמה חשוב לך שתהיה מעניינת וייחודית? | ציר נפרד מ-5, לא בהכרח הפוך בול |
| 7 | עד כמה חשוב לך שיהיה לך אישית ברור למה זו קשירה הגיונית? | לא מניקד שיטה, אלא קובע כמה הסבר להראות |

### שאלות חדשות שנוספו (מבוססות על השיחה השנייה)

| # | שאלה | מדוע נוספה |
|---|------|-----------|
| 8 | עד כמה חשוב לך לצאת ידי חובה לפי כמה שיותר שיטות בבת אחת? | עלתה מפורשות בשיחה 2 כשאלה נפרדת מ"נפוצות" |
| 9 | עד כמה חשוב לך שלשיטה יהיה "גב" - ראשון/מקור ספציפי, ולא מיקס שהומצא מאוחר? | השאלה השלישית ה"מכוונת" שאוריאל שאל |
| 10 | עד כמה חשוב לך שהשיטה תהיה שמרנית ביחס למה שרגיל אצלך? | השאלה הראשונה ה"מכוונת" - כרגע ממומשת כקירוב לציר הנפוצות, ראו מגבלות בהמשך |
| 11 | האם מקובל עליך לשקול שיטות של פוסקים בני זמננו, או להישאר בקלאסי בלבד? | בשיחה 2 זה הוכרע מהניקוד ל-3/4, אבל עדיף לשאול במפורש ולא להסיק |
| 12 | האם יש סיבה להטות את ההחלטה לכיוון קהילה/פוסק מועדף? (רב-ברירה: מנהג הגר"א, חסידות, קבלה, דמיון לקשירה בלבן, העדפת הרמב"ם כפוסק, ...) | עלה בשיחה 2 ("בגדול חסידי זה דווקא רלוונטי... השאלה איזו חסידות"); הורחב לרשימת הטיות גנרית כדי לאחד כמה שיקולי "פוסק מועדף" תחת שאלה אחת |
| 13 | האם כבר קנית חוט תכלת שצריך להתאים אליו? | עלה בשיחה 2 ("שישלח תמונה של המוצר שקנה") - כרגע רק נשמר, לא משפיע על ניקוד |

### שאלות משנה (מוצגות רק בתנאי)

- **מספר כריכות (7/13)** - מוצגת רק אם לאחת מהמועמדות המובילות יש כמה גרסאות
  אורך (כמו ראב"ד או רמב"ם).
- **הכרעה חזותית** ("מה הכי יפה בעיניך?") - מוצגת רק כששתי המועמדות המובילות
  קרובות מאוד בניקוד. הבחירה כאן **דורסת** את ציון היופי המחושב, בדיוק כמו
  שקרה בפועל בשיחה 2 כשההכרעה הסופית התבססה על מבט בתמונות.

## 4. מודל הנתונים

### התובנה המרכזית: שיטה היא **הרכב**, לא יחידה אטומית

זו התובנה החשובה ביותר שעלתה מקריאת המקורות, והיא משנה את המודל מהיסוד.
הרב שמואל אריאל מנסח אותה במפורש ("התכלת וחידושה", פרק ז):

> "ניתן למנות בעניין זה חמישה דיונים שונים... מכיוון שיש כאן חמישה דיונים
> נפרדים, הרי מספר השיטות האפשריות הוא גדול מאד, שהרי כל שיטת קשירה צריכה
> לשלב הכרעה מסויימת בדיון א' יחד עם הכרעה מסויימת בדיון ב' וכן הלאה."

חמשת הדיונים:

| # | הדיון | רמת מחייבות |
|---|-------|-------------|
| א | מספר חוטי התכלת (1/8 רמב"ם, 2/8 ראב"ד, 4/8 תוספות) | **מעכב** |
| ב | צבע הכריכות (רוב תכלת / לסירוגין) | לכתחילה |
| ג | מספר החוליות (7 / 13) ומספר הכריכות בכל אחת | לכתחילה |
| ד | צורת החוליה (קשר כפול / תימנית) | נוי מצווה |
| ה | שילוב חמשת הקשרים עם החוליות | לכתחילה |

**רק דיון א' מעכב** - "אם הטיל מספר חוטי תכלת לפי דעה המצמצמת את מספרם, לא יצא
ידי חובה לפי הדעות הדורשות יותר... ואילו שאר העניינים אינם מעכבים, וכמעט בכל
אופן שעשה, יצא ידי חובה לפי כל הדעות". זו הבחנה קריטית למוצר: המשתמש צריך
לדעת על מה באמת שווה להתלבט.

המבנה הזה תואם במדויק את התרשים שאוריאל הכין (שמור ב-`content/diagram/`),
שבו הזרימה היא: תכלת? ← זיהוי החילזון ← מספר חוטי תכלת ← ליפופים ← תוספת
חמישה קשרים ← קשרי ביטחון.

לכן המודל מפריד עכשיו בין **שתי שכבות שונות לגמרי**:

- **`KnotComposition`** (`model/Composition.kt`) - השכבה **העובדתית**: ממה
  השיטה מורכבת בפועל, כהכרעה בכל אחד מחמשת הדיונים.
- **`axisScores`** (ב-`KnotMethod`) - השכבה **הסובייקטיבית**: כמה השיטה
  "יפה"/"נפוצה"/"מבוססת", לצורך התאמה להעדפות המשתמש בשאלון.

ההפרדה הזו היא מה שיאפשר בהמשך את כל ההרחבות המשמעותיות: סינון לפי הכרעה
קודמת (אזור "כמה" מזין את אזור "איך"), הצגת הוראות קשירה מדויקות, השוואת
שיטות דיון-מול-דיון, ואפילו בניית הרכב מותאם אישית שאינו תואם אף שיטה מוכרת -
בדיוק כפי שהרב אריאל עצמו עשה.

### שיטת קשירה (`KnotMethod`)
לכל שיטה: שם, תיאור קצר/מלא, "הערת עריכה" חופשית (משמרת את הסגנון האישי של
"שיקולים בשלוף" מהשיחות), תקופה (גאונים/ראשונים/אחרונים/בן-זמננו), **תגיות
זיקה** (`affinityTags` - קהילה כמו חב"ד/רדזין, אבל גם פוסק מועדף כמו
"posek-rambam", נטייה קבלית, דמיון לקשירה בלבן וכו', ראו שאלה 12 למעלה),
**הרכב הלכתי** (`composition` - ראו למעלה), **ציוני צירים** (0-10 לכל אחד
מ-8 הצירים שהשאלות מודדות), גרסאות אורך (variants), ומקורות
(כותרת/מחבר/ציטוט/קישור). **תמונות אינן שדה ב-JSON בכלל** - הן מתגלות
אוטומטית בזמן ריצה מתוך `assets/images/<method-id>/` (ראו `AssetImages.kt`
וסעיף 8 - "מנגנון תוכן `content/`").

כרגע יש 11 שיטות: רמב"ם, ראב"ד, החינוך, הגר"א, מנהג חסידי/קבלי, שיטת הערוך,
הרב יעקב נגן, תוספות, ר' עמרם גאון, הרב שמואל אריאל, ו"כמו בלבן". ארבע
האחרונות נוספו מהתרשים ומהמקורות.

הציון של שיטה מול צירים הוא הבחירה הטכנית המרכזית: זה מה שמאפשר לנקד כל שיטה
מול כל שאלה בלי לכתוב לוגיקה נפרדת לכל צירוף. **הציונים הראשוניים שמולאו כרגע
הם הערכה שלי על סמך הניסוחים בשיחות - הם טיוטה לכיול, לא קביעה סופית.**

### שאלה (`Question`)
טקסט, סוג (סליידר/בחירה יחידה/רב-ברירה/כן-לא), שלב (ראשי/משני), הציר שהיא
מנקדת (אם יש), ואפשרויות בחירה (לשאלות שאינן סליידר).

### מנוע הניקוד (`ScoringEngine`)
1. סינון קשיח (למשל: הסרת שיטות של פוסקים בני זמננו אם המשתמש ביקש "קלאסי בלבד").
2. סכום משוקלל: לכל שאלת סליידר, `משקל = ערך/10`, ותרומה = `משקל * ציון_השיטה_בציר`.
   הציון הסופי מנורמל ל-0-100 ("אחוז התאמה").
3. בונוס הטיה: שאלה 12 (`q_affinity_bias`) היא רב-ברירה, אז הבונוס פרופורציונלי -
   יחס התגיות שהמשתמש בחר שהשיטה אכן מקיימת, לא "הכל או כלום".
4. יצירת הסבר קצר אוטומטי (הצירים המובילים + הערת העריכה של השיטה) - עונה על
   שאלה 7 (בהירות אישית).

### שמירה מקומית
- **תוכן הבסיס** (שיטות + שאלות) יושב כקבצי JSON בתוך `assets/`, לא בבסיס נתונים.
  הסיבה: זה תוכן שאתה (אוריאל) עורך ידנית עם מקורות/תמונות - JSON קריא ונוח
  לעריכה ישירה, בלי צורך במיגרציות DB. כשהכמות תגדל משמעותית או שיהיה צורך
  בחיפוש/סינון כבד, אפשר לעבור ל-Room.
- **היסטוריית ייעוצים** (`SavedConsultation`) נשמרת במכשיר בעזרת DataStore -
  כדי לתמוך במקרה השימוש שבו אתה מריץ את השאלון עבור כמה חברים לאורך זמן,
  ורוצה להיזכר מה הומלץ למי ומתי. אין שרת מרוחק - הכל מקומי בשלב הזה.

## 5. מסכי הממשק

0. **בית עליון (Topics)** - בורר בין חמשת האזורים, שם המתייעץ, וכניסה למסע (ראו סעיף 2).
1. **בית "איך"** - הסבר קצר, כפתור "התחל שאלון", וכניסה ל"עיון חופשי בשיטות" ול"ייעוצים קודמים".
2. **שאלון** - כל שאלות השלב הראשי במסך אחד גלול (לא שאלה-בכל-מסך), עם כפתור
   "הצג תוצאות" קבוע בתחתית. סליידר 1-10 / כפתורי בחירה יחידה / רב-ברירה /
   כן-לא, לפי סוג השאלה. שאלות סליידר מקבלות ערך ניטרלי (5) כברירת מחדל.
3. **שאלת משנה: מספר כריכות** - מוצגת רק בתנאי (ראו למעלה).
4. **שאלת משנה: הכרעה חזותית** - כרטיסים של המועמדות המובילות עם תמונות
   אמיתיות אם קיימות (`AssetImages`), אחרת placeholder כללי; בחירה קובעת
   את הסדר הסופי.
5. **תוצאות** - רשימת שיטות מדורגת, לכל שיטה: אחוז התאמה, תקציר, הסבר קצר
   אוטומטי, וכפתורים "פרטים ומקורות" / "זו הבחירה שלי".
6. **פירוט שיטה** - תיאור מלא, הערת עריכה, גרסאות אורך, רשימת מקורות, וכל
   התמונות שנמצאו לשיטה (או placeholder אם אין).
7. **ספריית שיטות** - כל השיטות לעיון חופשי, בלי לעבור שאלון (שימושי גם
   כמסך "לימוד" עצמאי).
8. **ייעוצים קודמים** - רשימת ייעוצים שמורים לפי שם/תאריך/שיטה שנבחרה.

## 6. מגבלות ידועות בגרסה הזו (v0.1)

- ציוני הצירים לכל שיטה הם טיוטה ראשונית שלי, לא כיול מדויק - צריך לעבור
  עליהם יחד ולתקן.
- שאלת ה"שמרנות" (עד כמה קרוב למה שרגיל אצלך) ממומשת כרגע כקירוב לציר
  הנפוצות הכללי, כי אין עדיין דרך לדעת מה בדיוק "רגיל" אצל כל משתמש. אפשרות
  לשיפור: לשאול את המנהג הביתי/משפחתי הספציפי ולהצליב מול תגית מתאימה בכל שיטה.
  שאלה 13, "האם כבר קנית חוט", נשמרת אך עדיין לא משפיעה על הניקוד. שלב הבא:
  לצרף זיהוי יצרן/מוצר ולוודא התאמה טכנית.
- שיטות חסידיות (חב"ד/רדזין/אחרות) מקובצות כרגע לכניסה אחת גנרית - יש לפצל בהמשך.
- **מנגנון התמונות מוכן, אך אין עדיין תמונות בפועל** - תיקיות `content/images/<id>/`
  קיימות אבל ריקות; כל שיטה מציגה כרגע placeholder כללי. גם מקורות מלאים
  עדיין חסרים - כל שיטה מסומנת בפירוש היכן חסר תוכן.
- **מיפוי תגיות ה"הטיה" (`affinityTags`) בשיטות הוא טיוטה ראשונית שלי** (למשל
  אילו שיטות מסומנות כ"posek-rambam" או "white-resemblance") - נעשה בלי
  תוכן מפורט, ויש לכייל יחד כשיגיע.
- **ההרכבים (`composition`) מולאו מהמקורות אך טעונים אימות מולך.** מילאתי
  לפי התרשים שלך, מאמר הרב אריאל ו"שער כריכת התכלת" של הרב רפמן. הפריטים
  הכי פחות בטוחים: **שיטת הערוך** (מתוארת כ"רמב"ם עם חוט ראב"ד" - לא היה
  לי ברור אם הכוונה למספר החוטים או לאורך, ולכן השארתי את `knotScheme` ריק),
  ו**ראב"ד** (רצף הכריכות 7/?/7/7, כשה"?" הוא ה"אין כמות מוגדרת" שבתרשים).
  זה בדיוק סוג הדבר שכדאי שנעבור עליו בסשן השאלות והתשובות.
- **אזורי "האם" ו"כמה"** (ראו סעיף 2) עדיין לא שאלון אמיתי - יש להם תוכן
  אמיתי (ב-`Topic.kt`), אבל לא מנוע החלטה או מסך אינטראקטיבי.
- **האזורים עדיין לא "מדברים" זה עם זה.** מי שהכריע ב"כמה" עדיין לא רואה
  את ההשפעה ב"איך". זו התשתית המשמעותית הבאה שחסרה.
- **מנגנון תוכן `content/`** (תמונות + `texts.txt`, ראו `content/README.md`
  וסעיף 8) נבנה ותוקן שהוא מתקמפל, אבל לא נבדק בפועל מקצה לקצה (אין עדיין
  תמונה או בלוק טקסט אמיתי שרץ דרכו) - שווה לבדוק את זה יחד ברגע שתוסיף
  תמונה או טקסט ראשון.
- **הפלטה** כווננה ויזואלית לפי התצלומים ששלחת (לא נדגמה בכלי דיוק כלשהו) -
  אם היא לא מספיק קרובה לגוון האמיתי, קל לכוונן (כל הצבעים במקום אחד ב-
  `Theme.kt`).
- **הבנייה עצמה כן אומתה בפועל, דרך CI.** בסביבת הסשן האינטראקטיבי
  `dl.google.com` (שממנו Gradle מוריד את Android Gradle Plugin ואת Android
  SDK) חסום ברמת מדיניות הרשת (403 על ה-CONNECT), כך שלא ניתן להריץ
  `./gradlew assembleDebug` ישירות כאן. לכן נוסף workflow
  (`.github/workflows/build-debug-apk.yml`) שרץ על runner של GitHub
  (עם גישה תקינה ל-Maven ול-SDK) בכל push ל-`main`/`claude/**`, ומריץ שם
  `assembleDebug` בפועל. ה-workflow רץ בהצלחה - ה-APK שנבנה מתפרסם גם
  כ-artifact של ה-run וגם לענף `builds` (נדרס בכל הרצה). אין עדיין בדיקות
  יחידה (unit tests) בפרויקט - `assembleDebug` מוודא רק שהקוד מתקמפל, לא
  נכונות לוגית. כשתפתח את הפרויקט ב-Android Studio, עדיין כדאי להריץ ולבדוק
  ידנית את זרימת המשתמש בפועל.

## 7. מה אני צריך ממך כדי להתקדם

1. **כיול ציוני הצירים** לכל שיטה ב-`methods.json` - הכי משמעותי להמשך.
2. **מקורות מדויקים** (ציטוט + קישור, למשל לספריא) לכל שיטה.
3. **תמונות** של כל שיטת קשירה - פשוט לגרור לתוך `content/images/<method-id>/`
   בריפו (ראו `content/README.md`), האפליקציה מוצאת אותן אוטומטית.
4. **פיצול שיטות חסידיות** לערכים נפרדים (חב"ד, רדזין, ואחרות) עם פירוט.
5. **החלטה** לגבי הכללת שיטת הרב יעקב נגן ושיטת הערוך (רמב"ם+חוט ראב"ד) -
   כרגע קיימות במודל אך מסומנות כ"מיוחדות"/"פוסק בן זמננו".
6. משוב על **התרגום המספרי** של השאלות המכוונות (למשל אם יש דרך טובה יותר
   לנקד "שמרנות" מאשר קירוב לנפוצות).
7. **אוסף הקבצים** שהוזכר (טקסטים על שיטות, תמונות, גרף שיטות קיים, טקסטים
   חופשיים נוספים) - ברגע שיגיע, ישמש לכיול הסעיפים 1-3 למעלה ולתוכן של
   אזורי "האם"/"כמה"/"הלכות ושאלות נפוצות".

## 8. מצב הקוד

נוצר שלד פרויקט Android (Kotlin + Jetpack Compose) תחת `app/`:

```
content/                      תוכן שמוזרם לאפליקציה - ראו "מנגנון תוכן" בהמשך
  README.md                   הסבר מלא על הפורמטים
  images/<method-id>/         תמונות קשירה (ריק כרגע, מוכן להוספה)
  texts.txt                   טקסטים חופשיים שדורסים שדות ב-methods.json

app/src/main/
  assets/methods.json         שיטות (תוכן ראשוני מהשיחות)
  assets/questions.json       שאלון מלא (ראשי + משני)
  res/drawable/ic_knot_placeholder.xml  placeholder לשיטה בלי תמונה
  java/org/tekhelet/knotadvisor/
    model/Models.kt           כל מודלי הנתונים
    model/Topic.kt            ארבעת אזורי הייעוץ + roadmap (סעיף 2)
    logic/ScoringEngine.kt    מנוע הניקוד
    data/ContentRepository.kt טעינת JSON + החלת overrides מ-texts.txt
    data/AssetImages.kt       גילוי תמונות בזמן ריצה מ-assets/images/<id>/
    data/HistoryStore.kt      שמירת ייעוצים (DataStore)
    ui/AppViewModel.kt        מצב האפליקציה (אזור "איך")
    ui/components/KnotDivider.kt   מוטיב עיצובי (פס ליפופים כחול-לבן)
    ui/screens/TopicsHomeScreen.kt  בית עליון - בורר בין 4 האזורים
    ui/screens/TopicNotesScreen.kt  תוכן לאזורים שאין בהם עדיין שאלון
    ui/screens/JourneyScreen.kt     המסע בין התחנות
    ui/screens/CompositionBuilderScreen.kt  בונה ההרכב האישי
    ui/screens/MethodTreeScreen.kt  מפת השיטות (התרשים, לניווט)
    ui/screens/LookPreferenceScreen.kt  "מה יפה בעיניך"
    ui/screens/TyingGuideScreen.kt  מה לקנות ואיך לקשור
    ui/screens/FeedbackScreen.kt    שליחת פידבק
    logic/GadilBuilder.kt     רצף הגדיל - מזין ציור והוראות כאחד
    logic/TyingInstructions.kt  הוראות קשירה מילוליות
    logic/CompositionCoherence.kt  מתחים פנימיים בהרכב (בלי לפסול)
    logic/Products.kt         המוצרים לקנייה וכלל האצבע
    logic/ThreadLength.kt     מחשבון "יש לי מספיק חוט"
    ui/components/TzitzitVisual.kt  ציור הגדיל
    ui/screens/HowHomeScreen.kt     בית אזור "איך"
    ui/screens/MethodDetailScreen.kt  כולל MethodImages (רינדור תמונות/placeholder)
    ui/screens/*.kt           שאר מסכי אזור "איך" שתוארו למעלה
    ui/navigation/NavGraph.kt ניווט בין המסכים
    ui/theme/Theme.kt         ערכת נושא (סקאלת כחול-לבן, בהשראת תצלומי חוט)
    MainActivity.kt
```

לפתיחה: לייבא את תיקיית השורש ל-Android Studio (Gradle יזהה את `app` module
אוטומטית דרך `settings.gradle.kts`). מינימום SDK 26, קומפילציה מול SDK 34.

### מוסכמות הציור של הגדיל

הציור ב-`TzitzitVisual` אינו דקורטיבי - הוא מנסה לייצג את הקשירה בפועל, לפי
מוסכמות שאוריאל הגדיר:

| מה | איך מצויר |
|----|-----------|
| קשר כפול | ✕✕ - שני X זה מתחת לזה |
| חוליה תימנית | חצי כריכה ימנית, שתי כריכות באלכסון היורד משמאל לימין, וחצי כריכה שמאלית. מבנה אחד באורך שלוש כריכות |
| חוליה תימנית משולבת (ראשונה/אחרונה) | החלק הלבן ככריכה "שבורה", והחלק בתכלת כמבנה התימני הרגיל |
| חוליה תימנית הפוכה | קו אלכסוני על גבי הכריכות. יכולה להיות באורך 1, 2 או 3 כריכות, כי בשיטה החסידית קשר כפול חותך אותה באמצע |
| רווח חוליה (רמב"ם) | רווח באורך חוליה שדרכו רואים את החוטים המאונכים שסביבם כורכים |

אותו מנוע (`GadilBuilder`) מזין גם את הציור וגם את ההוראות המילוליות
(`TyingInstructions`), כדי שהם לא יוכלו להיפרד זה מזה.

### חתימת ה-APK ועדכון במקום

**הבעיה שהיתה:** כל התקנה של גרסה חדשה דרשה להסיר קודם את הישנה. הסיבה:
GitHub Actions מייצר `~/.android/debug.keystore` **חדש בכל הרצה**, ולכן כל
APK נחתם במפתח אחר. אנדרואיד מסרב לעדכן אפליקציה מותקנת כשהחתימה לא זהה
לחתימה של ההתקנה הקיימת - זו הגנת אבטחה בסיסית, לא באג.

**הפתרון:** נוצר מפתח חתימה קבוע ב-`keystore/tekhelet-dev.jks`, והוא נשמר
בריפו ומשמש את שני סוגי הבנייה (`debug` ו-`release`). בנוסף, `versionCode`
נגזר מ-`GITHUB_RUN_NUMBER` כך שכל בנייה ב-CI מקבלת מספר גבוה יותר מקודמתה.

מכאן והלאה: **מספיק להתקין את ה-APK החדש על הישן** - הוא ידרוס אותו.
(פעם אחת אחרונה תצטרך להסיר ידנית את הגרסה שכבר מותקנת אצלך, כי היא נחתמה
במפתח הישן והאקראי. מאותה נקודה זה יעבוד חלק.)

**הערת אבטחה, כדאי שתדע:** מפתח חתימה פרטי ששמור בריפו ציבורי הוא מפתח
שכל אחד יכול להשתמש בו. בפועל, המשמעות היחידה כאן היא שמישהו יכול לייצר
APK שאנדרואיד יסכים להתקין *על גבי* האפליקציה הזו - אבל רק אם ישכנע אותך
להתקין אותו ידנית מלכתחילה. לאפליקציה אישית שאינה בחנות, זה סיכון קטן,
ובחרתי בו כי הוא הפתרון היחיד שעובד בלי שתצטרך להגדיר כלום. **אם תרצה
לסגור גם את זה** - אפשר להעביר את המפתח ל-GitHub Secret (מקודד ב-base64),
לפענח אותו בזמן ה-workflow, ולהוציא את הקובץ מהריפו. זה דורש הגדרה חד-פעמית
שלך בהגדרות הריפו, ואשמח לעשות את זה אם תבקש. **וכשנגיע לפרסום אמיתי
(חנות/הפצה רחבה) - חובה להחליף למפתח release אמיתי שאינו בריפו.**

### מנגנון תוכן `content/`

כדי לאפשר הוספת תמונות וטקסטים בלי לגעת ב-JSON/קוד, `content/` בשורש הריפו
(מחוץ ל-`app/`) הוא תיקיית עריכה נפרדת:

- **תמונות**: `content/images/<method-id>/<כל שם קובץ>` - גם כמה תמונות
  לשיטה אחת. `AssetImages.kt` מגלה אותן **בזמן ריצה** (לא ממש "בזמן
  קומפילציה" - זה פשוט יותר ואמין יותר, ראה למטה) מתוך `assets/images/`.
  אם אין תמונה, `MethodImages` (ב-`MethodDetailScreen.kt`) מציגה placeholder
  כללי (`ic_knot_placeholder`). התמונות עצמן נטענות עם Coil
  (`io.coil-kt:coil-compose`), דרך URI מסוג `file:///android_asset/...`.
- **טקסטים**: `content/texts.txt` - קובץ יחיד עם בלוקים `### <method-id>.<field>`.
  `ContentRepository` מפרסר אותו ומחיל override על `shortSummary`/
  `fullDescription`/`editorialNote` של השיטה המתאימה, מעל מה שכתוב ב-JSON.
- **הסינכרון**: שני Gradle tasks חדשים ב-`app/build.gradle.kts`
  (`syncContentImages`, `syncContentTexts`) מעתיקים מ-`content/` לתוך
  `app/src/main/assets/` לפני כל build (`preBuild.dependsOn(...)`). הקבצים
  המועתקים לא נשמרים ב-git (נוספו ל-`.gitignore`) - `content/` הוא מקור
  האמת היחיד.

**הערה טכנית**: ביקשת "שנקרא ומפורסר בזמן הקומפילציה" - בפועל ה-Copy
(העתקה ל-assets) קורה בזמן ה-build, אבל הפירסור בפועל (המרת הטקסט לשדות)
קורה כשהאפליקציה נטענת במכשיר, לא ב-Gradle עצמו. בחרתי בגישה הזו במכוון:
כתיבת קוד גנרציה ב-Gradle (Kotlin DSL / Groovy) קשה יותר לבדוק בלי להריץ
Gradle מקומית (וזה חסום בסביבה האינטראקטיבית שלי, ראו סעיף 6) - בעוד
שפירסור ב-Kotlin רגיל בתוך האפליקציה הוא קוד רגיל שאפשר לבדוק ולתקן כמו כל
קוד אחר. התוצאה הסופית זהה מבחינתך: מוסיפים קובץ, בונים, זה מופיע באפליקציה.
