# תוכנית פיתוח - בורר קשירת תכלת

מסמך זה נכתב אחרי קריאת ששת המקורות שאוריאל שלח (ראו "מקורות" בסוף), והוא
משלים את `DESIGN.md`: שם מתועד **מה קיים**, כאן מתועד **מה הלאה ולמה**.

התוכנית מחולקת לפי **דחיפות ותלות**, לא לפי גודל. הסדר בתוך כל קטגוריה הוא
סדר העדיפות המוצע.

---

## קטגוריה 0: כבר בוצע בסבב הזה

מפורט כאן כדי שיהיה ברור מה כבר לא צריך לתכנן.

- **מודל ההרכב** (`KnotComposition`) - חמשת הדיונים, עם רמת מחייבות לכל אחד.
  זו התשתית שכל השאר נשען עליה. ראו `DESIGN.md`, "שיטה היא הרכב".
- **11 שיטות** במקום 7 - נוספו תוספות, ר' עמרם גאון, הרב שמואל אריאל, ו"כמו בלבן".
- **תצוגת ההרכב** במסך פירוט שיטה, עם תגית "מעכב"/"לכתחילה"/"נוי מצווה".
- **תוכן roadmap אמיתי** לשלושת האזורים שטרם מומשו.
- **עדכון APK במקום** - מפתח חתימה קבוע + `versionCode` עולה.
- **ארכוב התרשים** ב-`content/diagram/`.

---

## קטגוריה 1: לעשות עכשיו - זול, ברור, ומשפר מיד

דברים שאין בהם החלטה פתוחה ואפשר פשוט לבצע.

### 1.1 בדיקות יחידה למנוע הניקוד
כרגע **אין ולו בדיקה אחת** בפרויקט. `assembleDebug` מוודא רק שהקוד מתקמפל.
זה הפער הטכני הגדול ביותר: כל שינוי במנוע הניקוד הוא כרגע שינוי עיוור.
מינימום נדרש: נירמול הציון ל-0-100, בונוס ההטיה היחסי, הסינון הקשיח של
"קלאסי בלבד", וזיהוי מצב "תיקו" שמפעיל את ההכרעה החזותית.

### 1.2 טעינת התוכן להיות עמידה לשגיאות
`ContentRepository` קורא JSON ישירות. אם `methods.json` יישבר (וזה יקרה -
אתה תערוך אותו ידנית), האפליקציה תקרוס בהפעלה בלי הסבר. צריך `try/catch`
עם מסך שגיאה שאומר איזה קובץ ומה השורה.

### 1.3 בדיקת תקינות ל-JSON ב-CI
לוודא בזמן ה-build שכל `composition` מפנה ל-enum קיים, שאין `id` כפול,
ושכל תיקיית תמונות תואמת ל-`id` אמיתי. תופס טעויות עריכה לפני שהן מגיעות למכשיר.

### 1.4 ניווט אחורה ו-`TopAppBar`
כרגע אין כפתור חזרה באף מסך פנימי - רק כפתור המערכת. זה מרגיש שבור.

### 1.5 שאלה 7 ("בהירות אישית") לא עושה כלום
היא מוגדרת כקובעת "כמה הסבר להראות", אבל בפועל לא מחוברת לשום דבר.
או לחבר אותה (הסבר קצר מול מורחב בתוצאות), או להסיר.

---

## קטגוריה 2: לעשות מוקדם - כי אחר כך זה יקר

שינויים מבניים. ככל שיצטבר יותר תוכן וקוד מעליהם, כך יקר יותר לשנות.

### 2.1 לפצל את `methods.json` לקובץ-לשיטה
קובץ אחד שמכיל 11 שיטות (ובעתיד 15+) עם תיאורים ארוכים הוא סיוט לעריכה
ידנית ול-diff. `content/methods/<id>.json` יהיה נעים בהרבה, ומתאים לאופן
שבו אתה כבר עורך תוכן ב-`content/`.

### 2.2 האזורים צריכים לדבר זה עם זה
הזרימה בתרשים שלך היא **צינור**: תכלת? ← זיהוי ← מספר חוטים ← ליפופים.
כרגע ארבעת האזורים ב-`Topic` הם איים מנותקים. צריך מודל של **מצב הכרעה
מתמשך** - מה המשתמש כבר הכריע - כך שאזור "איך" יוכל לומר "בחרת רמב"ם
ב'כמה', ולכן השיטות האלה כבר לא רלוונטיות". זו התשתית הכי חשובה שטרם נבנתה.

### 2.3 להעביר את `Topic` מ-enum לתוכן
כרגע ה-roadmap של כל אזור מקודד ב-Kotlin. ברגע שתרצה לערוך אותו בעצמך,
הוא צריך לעבור ל-`content/`. אותו שיקול שהוביל ל-`texts.txt`.

### 2.4 להחליט מה קורה עם `axisScores` מול `composition`
עכשיו יש שתי שכבות. חלק מהצירים (`RISHONIM_BASIS`, `CONSENSUS_COVERAGE`)
בעצם **נגזרים** מההרכב ולא צריכים ניקוד ידני: `CONSENSUS_COVERAGE` הוא
פונקציה של מספר חוטי התכלת (4/8 מכסה יותר שיטות), ו-`RISHONIM_BASIS` נגזר
מהתקופה. שווה לגזור אותם אוטומטית במקום לכייל ידנית - פחות עבודה עבורך
ופחות מקום לסתירות.

### 2.5 מבנה נתונים למקורות
`SourceRef` הוא כרגע טקסט חופשי. אם רוצים קישורים אמיתיים (ראו 4.2),
צריך שדה מזהה מובנה (למשל `sefaria_ref: "Rambam, Tzitzit 1:7"`).

---

## קטגוריה 3: יכול לחכות

לא פחות חשוב - פשוט לא חוסם כלום.

### 3.1 שמירת ההיסטוריה כרגע דלה
`SavedConsultation` שומרת תשובות ותוצאה, אבל אין מסך שמציג את **הנימוק**
שניתן אז. אם אתה מייעץ לחברים לאורך זמן, תרצה לראות "למה המלצתי לו על זה".

### 3.2 ייצוא/שיתוף תוצאה
"שלח לחבר את מה שיצא לי" - כטקסט או תמונה. שימושי בדיוק בתרחיש שממנו
האפליקציה נולדה (אתה מייעץ למישהו בוואטסאפ).

### 3.3 מצב כהה
מוגדר ב-`Theme.kt` אבל `KnotAdvisorTheme` מקבל `useDarkTheme = false` קשיח
ולא מתחבר להגדרת המערכת.

### 3.4 נגישות וגדלי טקסט
טקסט עברי ארוך בגדלים קטנים. שווה מעבר על `contentDescription` וגדלים.

### 3.5 מסך "השוואת שיטות"
עכשיו כשיש `composition`, אפשר להציג טבלה של שתיים-שלוש שיטות זו מול זו
לפי חמשת הדיונים. זה בדיוק מה שהתרשים שלך עושה - רק אינטראקטיבי.

---

## קטגוריה 4: דורש מידע או החלטה ממך

**זו הקטגוריה שסשן השאלות והתשובות שתכננת אמור לכסות.**

### 4.1 כיול - הכי חשוב
- **אימות ההרכבים.** מילאתי 11 הרכבים מהמקורות. שניים שאני הכי פחות בטוח בהם:
  **שיטת הערוך** (האם "רמב"ם עם חוט ראב"ד" מדבר על מספר החוטים או על האורך?)
  ו**ראב"ד** (מה בדיוק ה"אין כמות מוגדרת" בחוליה השנייה).
- **ציוני הצירים** - עדיין הערכה שלי. במיוחד לארבע השיטות החדשות שהוספתי.
- **תגיות ההטיה** - אילו שיטות באמת "מנהג הגר"א", אילו "דמיון ללבן".

### 4.2 מקורות מדויקים
לכל שיטה: ציטוט + הפניה. **הרחבה אפשרית:** ל-Sefaria יש API ציבורי
(`/api/v3/texts`) שמאפשר לשלוף טקסט מקור לפי הפניה מובנית. זה יאפשר
"לחץ כדי לראות את לשון הרמב"ם" בתוך האפליקציה, בלי להקליד ידנית. דורש
החלטה: האם האפליקציה תהיה מקוונת (שליפה חיה) או שנוריד מראש (offline).
**המלצתי: offline** - האפליקציה כולה מקומית היום, וזה יתרון אמיתי.

### 4.3 תמונות
המנגנון מוכן וממתין. `content/images/<id>/` - 11 תיקיות ריקות.
בתרשים שלך יש כבר 12 תצלומים; אם יש לך אותם כקבצים נפרדים, זה הדבר
היחיד שחסר כדי שהאפליקציה תיראה גמורה.

### 4.4 פיצול השיטות החסידיות
כרגע חב"ד ורדזין מקובצים לערך אחד. התרשים שלך מציג "חב"ד / ראדזין" יחד,
אז ייתכן שזה נכון - אבל צריך שתאשר.

### 4.5 החלטות מוצר פתוחות
- **האם "כמו בלבן" צריכה להופיע כשיטה לגיטימית?** הוספתי אותה כי היא
  בתרשים שלך, אבל גם הרב אריאל וגם הרב רפמן שוללים אותה מפורשות בתכלת.
  כרגע היא מסומנת `white-custom-not-gemara`. להשאיר? להציג עם אזהרה?
- **מה עושים עם זיהוי החילזון?** בתרשים שלך זה שלב נפרד (ארגמון מול
  ראדזין), ואצל הרב טל זה מאמר שלם. כרגע אין לו מקום במודל.
- **הרב שמואל אריאל כ"שיטה"** - הוספתי אותו כשיטה, אבל הוא בעצם *הרכב
  מנומק* ולא מסורת. אולי מגיע לו סוג אחר של ישות ("הרכבים מוצעים").

---

## קטגוריה 5: הרחבות פונקציונליות - חשיבה בגדול

לא מבוקש כרגע, אבל שווה שיהיה על השולחן, כי חלקן משפיעות על החלטות תשתית.

### 5.1 מחולל הוראות קשירה
**זו לדעתי ההרחבה בעלת הערך הגבוה ביותר.** ברגע שיש `composition` מובנה,
אפשר לייצר אוטומטית את רצף הפעולות: "קשר כפול ← כריכה בלבן ← 4 כריכות
תכלת ← קשר כפול ← ...". בדיוק כמו "סיכום מעשי" של הרב אריאל ו"הנחיות
לענ"ד" של הרב רפמן - אבל מותאם לשיטה שנבחרה. זה הופך את האפליקציה מ"יועץ
בחירה" ל"כלי שמלווה אותך עד הציצית הקשורה".

### 5.2 בונה הרכב אישי
במקום לבחור שיטה מוכנה - לענות על חמשת הדיונים בנפרד ולקבל את ההרכב שלך,
עם ציון "השיטה הקרובה ביותר היא X". זה בדיוק מה שהרב אריאל עשה בפועל.
המודל כבר תומך בזה.

### 5.3 מצב "מדריך" מול מצב "יועץ"
האפליקציה נולדה מהתרחיש שבו **אתה** מייעץ לחבר. שווה מצב מפורש: "אני עונה
עבור מישהו אחר" - שמשנה ניסוחים, ושומר לפי שם.

### 5.4 ויזואליזציה של הגדיל
לצייר את הציצית לפי ההרכב (SVG/Canvas) - פסי תכלת ולבן, קשרים, חוליות.
נותן תשובה מיידית ל"איך זה ייראה", בלי להמתין לתצלומים.

### 5.5 חישוב אורך חוט נדרש
שאלה 13 ("כבר קנית חוט") כרגע נשמרת ולא עושה כלום. הרב רפמן מקדיש פרק
ל"מי שיש לו חוט תכלת קצר", ויש דין של י"ב אגודלים ושליש גדיל. אפשר לחשב
מההרכב אם החוט שקנית מספיק - וזו שאלה מעשית מאוד.

### 5.6 מצב לימוד
המקורות שקראתי הם חומר לימוד מצוין בפני עצמו. "ספריית שיטות" כבר קיימת;
אפשר להרחיב לסוגיות (שבע רקיעים וששה אווירים, היכר חוליות, חמישה קשרים).

---

## מקורות שנקראו

| מקור | מה יש בו |
|------|----------|
| "התכלת וחידושה", הרב שמואל אריאל | המאמר המרכזי ל"האם". פרק ז מגדיר את חמשת הדיונים - **הבסיס למודל ההרכב** |
| "שער כריכת התכלת", הרב אי"ש רפמן | כ"ב פרקים על הכריכה. שלוש דרכי חלוקה, גווני חוליות, והנחיות מעשיות |
| "זיהוי התכלת", הרב שמואל טל | ארבעה מסלולי ראיה לארגמון קהה קוצים |
| "רוחניות ומטא-הלכה בכריכות תכלת", הרב יעקב נגן (אקדמות כ"ז) | ההצעה שמופיעה כ-`rav-nagen` |
| שו"ת על צורת החוליות, הרב שמואל אריאל | תשובה מפורשת: אשכנזי/ספרדי לא יאמץ חוליות תימניות |
| "סיכום שיטות קשירת ציצית", אוריאל קורל | התרשים. **המפה המבנית של כל התחום** - שמור ב-`content/diagram/` |
| "הוראות שימוש לתיקיית קורס תכלת", אוריאל קורל | סדר קריאה, ושלוש השאלות (האם/כמה/איך) |
